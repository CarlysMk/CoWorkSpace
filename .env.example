# DB
DB_USER=cowork
DB_PASSWORD=coworkpass
DB_NAME=coworkdb


# JWT
JWT_SECRET=change_this_secret


# App
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:4000
# Payments (opzionale)
STRIPE_SECRET_KEY=

# Persistence note: when using docker-compose, the Postgres data is stored in the named volume 'cowork_db_data'.
# Your data (utenti, prenotazioni, sedi) resta salvata anche dopo aver fermato/riavviato i container.
# Per eseguire in locale senza Docker, imposta DATABASE_URL a un Postgres raggiungibile.
