{
  "name": "coworkspace-e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:pw": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  }
}